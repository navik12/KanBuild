# Generated database (rebuild it with: bash database/build.sh)
*.db
*.sqlite
*.sqlite3

# Note: analysis/project_report.csv is intentionally NOT ignored.
# It is committed so the Looker Studio dashboard (via a Google Sheet that
# pulls it from GitHub with IMPORTDATA) always has the latest data.

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor noise
.DS_Store
.vscode/
.idea/
